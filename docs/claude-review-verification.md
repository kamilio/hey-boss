# Claude review verification — 2026-09-15

Checked the ten findings pasted into the request against the current working tree. The supplied `/private/tmp/claude-501/.../ac4a2749d0f709574.output` file is no longer present, so its additional contents could not be inspected. Existing uncommitted changes were preserved.

| # | Verdict | Evidence and resolution |
|---|---|---|
| 1 | Already fixed in current code | Swift `Store.process` routes hide through `dismissRecords`, which cancels prompt/approval records and resolves waiters without a kind precondition. Existing Swift audits cover both question kinds; broker integration covers upstream ID translation and durable cancellation. |
| 2 | Confirmed compatibility gap; fixed | New commands were sent without a version check. Mac setup now records protocol version 1 after unloading the previous daemon; companion connection and local overview require that metadata before sending a protocol handshake. Legacy installations receive an upgrade error before any probe or tunnel is opened. The daemon answers the handshake; incompatible replies are rejected. SSH preparation records the negotiated protocol on the server; all broker forwarding, including telemetry, refuses an unnegotiated bridge. This also protects already-open legacy tunnels when the broker is upgraded. Companion-side overview uses that gate. |
| 3 | Already fixed in current code | Socket readers use bounded, deadline-aware `readRequest` and catch decoding errors. Added empty-peer and non-JSON socket-pair cases to the Swift audit. Empty input returns empty Data and decoding fails recoverably. |
| 4 | Confirmed; fixed | Client `StreamLocalBindUnlink` cannot clean a remote bind. Remote preparation now invokes a hidden CLI helper before reverse forwarding. It removes a socket only after `connect` reports connection refused; live sockets, symlinks, regular files and other errors are preserved/rejected. Unit coverage checks stale, live and regular-file paths. |
| 5 | Confirmed; fixed | `config()?` exited the retry loop before status updates. Invalid configuration now drops the session, publishes `config-error`, and retries in-process. A macOS subprocess test starts with stale connected status and malformed configuration, verifies the process stays alive, then repairs/disables configuration and verifies orderly stopped status. |
| 6 | Confirmed; fixed | Both forwarding paths held the queue mutex during tunnel I/O. They now load/decide under the mutex, forward without it, and reload/save under it. A stalled replay test verifies a second creation returns pending promptly. Cancellation during replay is preserved and a durable pending-hide flag retries cleanup of the delivered card. |
| 7 | Partly confirmed; fixed where needed | Waiters lacked client-liveness checks. The broker now checks response-side hangup/error between polls and releases client leases after disconnection. Tests distinguish request half-close from full close and exercise two rounds of 64 disconnected wait clients. Waiting through bridge failure remains intentional documented offline behavior, rather than an error exit. The unknown-ID SQLite precondition is already replaced by a thrown error in current Swift, covered by its resilience audit. |
| 8 | Confirmed; fixed | Resolving a supported symlink to an extensionless target lost the filename that had passed validation. The resolver now retains the absolute alias when its canonical target lacks a supported extension, so repeated validation is stable. Supported canonical targets still return their canonical path. The CLI icon test includes this symlink case. |
| 9 | Partly already fixed; remaining issue fixed | Current tests already filter/respond to agents_snapshot before accepting the expected operation. Added explicit `set_nonblocking(false)` on accepted streams before reading, including the shared bridge-action helper and replay-rejection mock. Tests pass on macOS and in a source test run on the Linux devbox server. |
| 10 | Confirmed; fixed | The project label width was fixed at 52 points. It now measures the label at its actual font, bounded to 150 points to retain title space. Swift audits verify frontend-web and frontend-api fit and the title remains separate and readable. Very long labels still truncate within the card's finite width. |

Validation: `cargo test --locked`; optimized Swift daemon and setup compilation; optimized Swift audit compilation and execution; `git diff --check`. Execution against a historical installed daemon was not exercised; its compatibility protection was verified by source inspection, installation gating, and unnegotiated-bridge tests. Deployment and live SSH verification are recorded below.


## Deployment and live verification

Rebuilt and replaced the existing Homebrew CLI, daemon and setup helper in place, registered the upgraded Mac daemon, and installed the upgraded companion on registered host devbox. History, settings and existing installation paths were preserved. Previous binaries, launch agents and a SQLite history backup are retained in `out/deployment-backup-20260915`.

The automatic connector was paused during upgrade and resumed with its existing enabled/VPN configuration. A deliberately stale bridge socket on devbox was cleaned during real SSH connection setup. Autoconnect reports connected, protocol version 1 is negotiated, and the Mac overview receives a live server snapshot.

Live Mac checks rejected zero-byte input, malformed JSON and missing task IDs, cancelled a hidden question, and answered the protocol handshake without changing the daemon PID. Live Linux-to-Mac checks verified durable question replay, translated remote hide, cached cancelled wait response, overview forwarding, and preservation of a live bridge socket by the stale cleanup helper.

The Rust source test suite also passed on the Linux server (macOS-only tests excluded). The original macOS suite, Swift audit, optimized builds and formatting checks passed before deployment.
