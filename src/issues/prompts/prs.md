Commit your changes, push a branch, open a pull request, and attach every PR with `hey-boss issue pr add {{number}} '<pr-url>'`.

If PR is ready (CI green), make the task "ready" which will unblock dependencies.
If you have dependency on other isue  - use stacked PRs on github on top
