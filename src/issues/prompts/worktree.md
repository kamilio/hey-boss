Work in a dedicated Git worktree for this issue at `{{worktree_path}}`, with branch `{{worktree_name}}` matching the directory name. Reuse that worktree and branch if they already exist, including when resuming this task; otherwise create them before editing files. Keep unrelated changes intact.

Create new worktrees with `git worktree add --lock --reason`, recording the project, issue and owning session in the reason. For an existing worktree, inspect its branch, HEAD, staged changes and lock reason first; lock it with `git worktree lock --reason` if it is unlocked and belongs to this task. Never replace another owner's lock. Keep the lock while work or validation is active, queued or interrupted. Record the path, branch, HEAD and validation receipts in issue progress so ownership is observable. A worktree lock does not reserve validation capacity.

Before resuming, inspect surviving work and the recorded validator process tree; do not reset staged changes or launch duplicate validators. If the directory or registration vanished, preserve surviving metadata and receipts, verify the existing branch, and resolve any surviving validator before restoring that branch to a locked workspace path. A branch cannot recover deleted uncommitted work. Only after verified completion and no live or queued descendants, perform explicit owner-authorized cleanup: retain receipts outside the checkout, unlock only this task's worktree, and remove it without force while retaining its branch. Never remove unrelated worktrees or override their locks.
